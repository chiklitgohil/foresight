# Decisions Log
Every judgment call gets one entry here, most recent at the top.
Format: `[Date/Session] Decision — Reasoning — Alternatives considered`

- [2026-07-07/Scaffolding] Ordered split ratio is 70/15/15 train/validation/test by `UDI` — PRD.md requires ordered train/validation/test to avoid future leakage from rolling features and forward labels, and ARCHITECTURE.md requires validation and held-out test files but did not specify exact ratios — Alternatives considered: 80/10/10 ordered split, 70/30 train/test only, random stratified split.
- [2026-07-07/Scaffolding] Dashboard framework is Dash — ARCHITECTURE.md defines a single `dashboard/app.py` demo surface, PRD.md scopes out production infrastructure, and Streamlit conflicted with the pinned `pandas==3.0.3` environment by requiring `pandas<3` — Alternatives considered: Streamlit, Flask.
- [2026-07-07/Architecture] Person A owns modeling and Person B owns product/demo — This minimizes concurrent edits because inference and dashboard can be built against the Section 5 mock contract while the model artifact is still pending — Alternatives considered: split by frontend/backend only, pair on all files.
- [2026-07-07/PRD] Primary imbalance strategy is class-weighted models — DATA_PROFILE.md found only 339 failures out of 10,000 rows, and PRD.md commits to class-weighted Logistic Regression and Random Forest before heavier imbalance techniques — Alternatives considered: SMOTE, focal loss.
- [2026-07-07/PRD] Ordered train/validation/test split by `UDI` is the evaluation strategy — DATA_PROFILE.md found `UDI` is monotonic and unique, and PRD.md requires leakage avoidance for rolling features and forward labels — Alternatives considered: random stratified split, both random and ordered splits.
- [2026-07-07/PRD] Risk bands are Green `<0.30`, Amber `0.30-<0.60`, Red `>=0.60` — PRD.md requires operator-facing bands instead of raw probability and ties each band to a maintenance action — Alternatives considered: equal thirds, threshold learned only after model evaluation.
- [2026-07-07/PRD] Target framing is failure within the next 50 operational cycles using `UDI` as pseudo-cycle order — DATA_PROFILE.md found no timestamp but did find monotonic `UDI`, so this creates a defensible hackathon prediction horizon without claiming real 24-hour time — Alternatives considered: point-in-time row classification, real-time horizon claim, NASA C-MAPSS stretch dataset.
- [2026-07-07/PRD] Failure modes are collapsed into binary `Machine failure` for the core model — DATA_PROFILE.md found only 339 failures and 24 rows with multiple mode flags, so the five failure modes are diagnostics rather than prediction-time subclasses — Alternatives considered: multiclass mode prediction, multilabel mode prediction.
